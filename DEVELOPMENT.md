# 开发指南

## 环境要求
- Node.js 22.x 或以上版本
- pnpm 10.x 或以上版本

## 安装依赖

在项目根目录下执行以下命令安装依赖：

```bash
pnpm install
```

## 启动调试

在项目根目录下执行以下命令启动调试：

```bash
pnpm dev
```

创建思源笔记软连接

在项目根目录下执行以下命令创建软连接：

```bash
pnpm makeLink
```

打开思源笔记，在插件管理中启用该插件。保持服务器不停止，可以热重载。